#include <stdio.h>
#include <stdlib.h>

int main() {
    // definindo variáveis
    int a = 5, b = 10, c = 15;

    // maior
    if(a > 2) {
        printf("\n %d eh maior que 2");
    }

    // < menor
    // <= menor ou igual
    // >= maior ou igual
    // != difernte
    // == igual
    return 0;
}
