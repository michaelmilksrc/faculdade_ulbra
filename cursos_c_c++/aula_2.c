#include <stdio.h>
#include <stdlib.h>

int main(){
    // criando variével
    int a = 5, b;

    // imprime a variavel
    printf("O valor da variavel eh: %d", a);

    // pula linha
    printf("\n");

    // mudando valor variável
    a = 15;

    // imprime a variável
    printf("O valor da variavel eh: %d", a);

    // pula linha
    printf("\n");

    // entrada via terminal (pula linha)
    scanf("%d", &b);

    // pula linha
    printf("\n");

    // imprimindo
    printf("Uhuu\n");

    // pausando
    system("pause");

    return 0;
}
