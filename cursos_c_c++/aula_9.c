#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    // definindo variáveis
    int a = 1, b = 10;

    while (a <= 10) {
        printf("\n%d", a);
        a++;
    }

    while (b >=1) {
        printf("\n%d", b);
        b--;
    }
    
    return 0;
}